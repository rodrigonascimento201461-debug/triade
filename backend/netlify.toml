[build]
command = "npm run build"
publish = ".next"

[build.environment]
NODE_VERSION = "20"

# TESTE TEMPORÁRIO: plugin desativado pra isolar causa do erro de build.
# [[plugins]]
# package = "@netlify/plugin-nextjs"
